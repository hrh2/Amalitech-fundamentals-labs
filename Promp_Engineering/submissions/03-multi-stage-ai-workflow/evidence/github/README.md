# Evidence needed here — MANUAL ACTION REQUIRED

Nothing in this folder is real yet. After a real end-to-end run, add:

1. A screenshot of the actual commit(s) in your GitHub repo (or the filed issue, on the failure path).
2. If the target repo has CI (e.g. GitHub Actions) configured, a screenshot of the CI run result
   triggered by the commit.
3. The commit SHA / issue URL, also recorded in `docs/run-log.md` Run 2.

Do not include a screenshot of a commit or CI run that didn't actually happen from this pipeline.
