# Evidence — Stage 2 (AI CLI)

`run-test-fixture-run-1.log` and `webhook-payload-test-fixture-run-1.json` in this folder are real,
generated by an actual `claude -p` headless run on 2026-09-03 (see `docs/run-log.md` Run 1). The
plan fed into that run was a hand-written test fixture, not a real WebStorm session — so this proves
the CLI mechanism, not a fully authentic pipeline run.

**MANUAL ACTION REQUIRED:** after you complete a real Stage 1 session, re-run
`scripts/run-stage2-cli.mjs` with your real plan and `--webhook-url` pointing at your live n8n
webhook. This will produce a new `run-<taskId>.log` / `webhook-payload-<taskId>.json` pair here —
add those as the authentic evidence and reference them from `docs/run-log.md` Run 2.
