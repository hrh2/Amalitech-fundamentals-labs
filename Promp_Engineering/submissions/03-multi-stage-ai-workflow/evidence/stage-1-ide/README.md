# Evidence needed here — MANUAL ACTION REQUIRED

Nothing in this folder is real yet. Add, from an actual WebStorm + AI Assistant session:

1. A screenshot or short screen recording of the AI Assistant chat panel open inside WebStorm, with
   the project tree visible (proves IDE-context awareness, not just a chat window).
2. The task description you gave it, and its response containing the structured plan JSON.
3. The exact plan JSON you then pasted into the n8n Stage 1 Intake form (save as `plan-<taskId>.json`
   here so it matches the run-log entry).

Do not include a screenshot of anything you did not actually do.
